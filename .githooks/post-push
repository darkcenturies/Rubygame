#!/bin/sh
# Runs after git push — deploy plugins from your PC (same path as WinSCP)
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT" || exit 0
if [ -f ".deploy.env" ]; then
  powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$REPO_ROOT/deploy.ps1"
fi
